/**
 * Component exports for edge case handling
 *
 * This file provides a centralized export point for all edge case
 * components, making it easier to import them into the main app.
 */

export { ErrorBoundary, withErrorBoundary } from './ErrorBoundary';
export { ErrorMessage, EmptyState, LoadingSkeleton } from './ErrorMessage';
export { SafeInput, SafeTextArea } from './SafeInput';
